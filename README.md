# CODE4hep build

## Installation

```bash
mkdir -p scratch
cd scratch
git clone git@github.com:code4hep/build
cd build
./setup.sh
```

## Usage

```bash
cd build
source init.sh
```

`init.csh` is provided for `tcsh` users.
